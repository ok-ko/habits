import React from "react";

function ResultMessage(value) {
	 // const {Report} = props;
console.log(value);
	return (
		//<div>
		// 	{Report.map((h) => <h4>{Report[h].date} :</h4>)}
		{value}
		// </div>

	)

		//
		// return (
		// {for (let i=0; i<Report.length;i++) {
		// 	<div>
		// 		<h4>{Report[i].date} :</h4>}
		// 	</div>

		// )}}
	// 	let Message = <h4>{Report[i].date} :</h4>};
	//
	// //
	// // 		<h4>{Report[i].date} :</h4>	}
	// // {Report[i].reportValues.map((h) => <h6>{h.title + (h.isDone?' +':' -')}</h6>))
	// // // 	return
	// // //
	// // // 	// updateStatMessage += ` ${Report[i].date}:${Report[i].reportValues.map((h) => h.title + (h.isDone?' +':' -'))}
	// // // 	//   `;
	// // // }
	// return (
	// 	<div>
	//
	// 		{/*<h4>date</h4>*/}
	// 		{/*<h6>habit1 -</h6>*/}
	// 		{/*<h6>habit2 + </h6>*/}
	// 		{Message}
	// 	</div>

}

export default ResultMessage;